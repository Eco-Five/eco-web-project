<div class="container mt-5">
    <!-- 게시글 상세 -->
    <div class="card p-4 mb-4">
        <h1 class="h4"> <%= board.title %></h1>
        <small class="text-muted"><%= board.name %> · <%= formatDate(board.board_date)%> · <i class="bi bi-eye"></i> 1,234</small>
        <p class="mt-3">
            <%= board.content %>
        </p>
        <div class="d-flex justify-content-between align-items-center mt-4">
            <span class="text-muted"><i class="bi bi-suit-heart-fill"></i> 좋아요 238</span>
            <div>
                <button class="btn btn-outline-primary btn-sm me-2" id="btn-update"><i class="bi bi-pencil"></i> 수정</button>
                <button class="btn btn-outline-danger btn-sm me-2" id="btn-delete"><i class="bi bi-trash"></i> 삭제</button>
                <button class="btn btn-outline-primary btn-sm me-2" id="btn-list"><i class="bi bi-list"></i> 목록</button>
            </div>
        </div>
        </div>
    </div>

    <% 
    function formatDate(dateStr) {
        const date = new Date(dateStr);
        const year = date.getFullYear();
        const month = String(date.getMonth() + 1).padStart(2, '0');  // months are zero-indexed
        const day = String(date.getDate()).padStart(2, '0');
        const hours = String(date.getHours()).padStart(2, '0');
        const minutes = String(date.getMinutes()).padStart(2, '0');
        
        return `${year}.${month}.${day} ${hours}:${minutes}`;
    }
    %>

    <script>
    //목록으로 돌아가기
    $("#btn-list").on("click", function(){
        location.href="/board"
        })
    
    //게시글 수정하기
    //현재 파일에서 수정하는 것이 아니고 수정화면을 띄워서 기존 정보를 가지고
    //수정하고자하는 값을 입력 받은 후에 저장하기(기존에 있는 정보를 수정하는 것.)
    //기존에 보이던 화면 URL이 새로운 URL로 변경되는 것이다.
    //기존에 요청은 끊어지고 새로운 요청이 일어난다.
    $("#btn-update").on("click", function(){
        location.href = `/board/update/<%= board.board_id %>`;  
        })

    //게시글 삭제하기
    $("#btn-delete").click((event)=>{
        const b_no = "<%= board.board_id %>"  // 삭제할 게시글의 ID
        // 서버로 AJAX 요청을 보내기
        $.ajax({
            url: `/api/board/${b_no}`,  // 서버의 삭제 API 경로
            type: 'DELETE',
            data: JSON.stringify({ b_no:b_no }),//요청 데이터 JSON으로 전달
            contentType:'application/json',//서버에 JSON 형식으로 데이터 전송
            success: function(response) {
                if (response.success) {
                    alert("삭제 완료되었습니다.");
                    location.href = "/board";  // 삭제 성공 시 목록 페이지로 이동
                } else {
                    alert("삭제 실패했습니다.");
                }
            },
            error: function(xhr, status, error) {
                alert("오류 발생: " + error);
            }
        });
    })
    </script>